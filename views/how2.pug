extends layout.pug

block content
  div(class='header')
    h3(class='category')
      a(href=categoryPath)= category
    if version
      h6(class='version') Version: #{version}
    if date
      h6(class='date') Date: #{date}
    h6 Tags: #{tags}
    h1= title
  div !{content}
