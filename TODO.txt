1) Make it installable as a global shell command
  
  $> npm install -g

2) Allow user to navigate and select a howto
3) Launch web server? and open url on selection
4) Consider switching to gulp4 alpha
5) Make building files (and watching) a command line option (will require gulp-runner)
